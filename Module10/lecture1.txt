MAP

STD::MAP<<K,V> CLASS
- a map is a non-continguous, template data structure that stores data in pairs
- A pair is made of two different parts;
	- A key: an identifier, some label for other parts of the pair
	- A valueL: the information being labled, the "real" data in the map
- internally, a map is implemented as a tpye of a binary tree ( you'll cover treees in later courses)
- They can very quickly store and retrieve elements

CREATING MAP VARIABLES

	map<key, value> someVariable;

- key: this is how you access it
- value: this is what we are really interested in.

	map<int, string> myMap;

- storing strings
- containers holding containers... sure whyt not?
-think of a box containing smaller boxes. a backpack xontaining a pencil cases, etc

ADDING DATA TO A MAP WITH EMPLACE()

	map <int , string> myMap; // interger keys, string values
	myMap.emplace(12, "bob"); // add a pair<int, string> to the map
	myMap.emplace(0, "superman")'; // Add a second pair

	map<string, int> characters;
	characters.emplace("spider-man", 17); // string ketys, integer values
	characters.emplace("yoda", 900) // another pair

ADDING AND MODIFIYING DATA WITH OPERATOR []
- the subscript operator can be used to add key/value pairs, or modify values

	map<int, string> myMap;
	// if the specified key doesn't already exist, creating a new
	// pair<int, string> with that key, then assign it a value
	myMap[41] = "Hello"; // create a pair<41, "hello">
	myMap[21] = "Thoir Odinson"; // creates a piar <25, "Thor Odison">

- You dont have to pre-allocate space for a map - it can do so dynamically.
- Maps are node based data structures like a linked list -- each pair is a node
- The subscript operator returns a reference to the value
- This allows for fast acces and modifications of existing values

MAP RETRIEVE VALUES BY KEY, NOT INDEX
- like vectors, the map has operator[] and at().
- They don't look up elements in array but return a value from a pair that has the provided key (possibly creating a new pair if one doesn't exists

	vector<int> numbers;
	// assume some initialization

	map<int, int> aMap
	// assume some initialization

	cout << numbers[0]; // print the first element
	cout << aMap[0]; // print  the value associated with KEY 0
	cout << numbers[numbers.size()-1]; // print the last element
	cout << aMap[aMap.size())-1] // ley might not exist
	cout << numbers[-20] a negative INDEX is always bad
	cout << aMap[-20]; // -20 is a valid key (but it might not exist in THIS map
	cout << numbers.at(-5) // throws an exception (out of range)
	cout << aMap.at(-5); // MIGHT throw an exception, if the key doesnt exist


USING STRINGS AS KEYS 
- computers can understand index values without issue - they (only!) understand nubmers
-  Humans tend to use words (strings) as labels instead of numbers.
- Consider writing a dictionary program:

KEYS IN A MAP MUST BE UNIQUE
- a map cannot store duplicat keys
- What if we wanted "multiple values" for a single key?
- You might not want to "blindly" use a given key - what if it doesn't exist
- someMap.find(key)
	- find() returns a iterator to the pair with a matching key.
	- if no match was found, returns an iterator to map::end()
- Iterators
	- A way of accessing elements in complex containers. Well cover this more as a standalone topic
- map:: end()
	- return a result you should never use directly, similar to nullptr fgor pointers. 

ITERATION THOUGH MAPS
- A typicall for loop most likely won't work when it comes to maps
- They keys of most map objects are will be incompatible, either by dta typers or values

ITERATORS
- a standard way of iterating through STL containers

	map<string, int> example;

	// create an iterator, using the same template types as the map
	map<string, int>::iterator iter;

	// set the interator to the beginning
	iter = example.begin();

	// as long as the iterator isnt at the end..
	// run the body of the loop, and move to the next element
	for (; iter != example.end(); ++ iter)
	{
		cout << iter->first;
		cout << iter->second;
	}

- Having an abstraction like begin() makes it easier to use in the class - you don't need to know all the interal details
- This process is the same for any kind of map - just change the data typpe of the iterator
- Much of this process is identicall for any containers that uses iterators

A MAP SOTS BASED ON KEYS
- the data is sorted by the key, in ascending order
- wanted to use map, but don't need sorted data? (sorting takes processing power!)

WHEN SHOULDNT WE USE MAPS
- if you dont have any specific key-value association
- If your "key" is really just an index
